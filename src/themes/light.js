/**
 * Theme colors are mirrored at '@/styles/colors/_colors.scss' for style tags.
 */

const light = {
    primary: '#ffdab9',
    secondary: '#556b2f',
    anchor: '#a0522d',
    background: '#ffa',
};

export default light;